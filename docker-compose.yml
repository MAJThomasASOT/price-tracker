services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "3001:3001"
    volumes:
      - sqlite_data:/app/data
    env_file:
      - ./backend/.env
    restart: unless-stopped

volumes:
  sqlite_data:
